name: Deploy Site

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # ЗАПУСК ТВОЕГО СКРИПТА
      - name: Generate static pages
        run: node generate.js

      # ПУБЛИКАЦИЯ
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: .          # Важно: публикуем весь корень, так как новости лежат в ./news
          force_push: true       # Чтобы перезаписать старые файлы
